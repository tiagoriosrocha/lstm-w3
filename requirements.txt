# Dependencias Python do projeto

# Base numerica e manipulacao de dados
numpy>=1.26,<3
pandas>=2.2,<3
pyarrow>=15,<20
matplotlib>=3.8,<4
tqdm>=4.66,<5

# Modelagem classica e metricas
scikit-learn>=1.4,<2
xgboost>=2.0,<3
lightgbm>=4.0,<5

# Deep learning
# Se quiser forcar CPU no Linux/Windows, pode instalar manualmente conforme a documentacao oficial:
# https://pytorch.org/get-started/locally/
torch>=2.2,<3

# Ambiente de notebooks
jupyterlab>=4,<5
notebook>=7,<8
ipykernel>=6.29,<7

# Observacao importante:
# O repositorio 3W e um dataset Git, nao um pacote Python instalavel via pip.
# Por isso, o clone do dataset deve ser feito separadamente com:
# git clone https://github.com/petrobras/3W.git
